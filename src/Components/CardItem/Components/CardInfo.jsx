/* eslint-disable no-unused-vars */
import style from '../CardItem.module.scss';

export default function CardInfo(props) {
  const { segments } = props;
  return (
    <div className={style.card__info}>
      <div className={style.card__infoItem}>i</div>
    </div>
  );
}
